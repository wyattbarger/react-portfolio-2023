import NavBar from  './NavBar'; // Add an import for my styled NavBar component. //

function Header () { // Add function to create a Header that contains our styled and 'routed' NavBar. // 
    return (
        <NavBar />
    );
}

export default Header; // Add a export default to send the Navbar comonent when imported by other files. //
